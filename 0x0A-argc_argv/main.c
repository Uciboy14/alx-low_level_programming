#include  "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * @argc: argument count                               * @argv: array of arguments
 *
 * Return: 0 for success, otherwise 1
 */
int main(void)
{
        printf("%d\n", _atoi("1002"));
        printf("%d\n", _atoi("1003"));
                                                              return (0);                                   }
